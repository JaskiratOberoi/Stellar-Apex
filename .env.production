# Production build config (committed — this is a public URL, not a secret).
# The Hostinger frontend calls the backend on this host via Caddy.
VITE_API_URL=https://api-apexhr.stellarinfomatica.com/api
